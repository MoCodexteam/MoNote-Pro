# تحليل تطبيق MoNote Pro

## 1. معلومات عامة
- **اسم التطبيق:** MoNote Pro
- **فريق التطوير:** MoCodex Team
- **الوصف الوظيفي:** تطبيق لإدارة الملاحظات الذكية سحابياً (Your smart notes in the cloud).
- **المنصة:** Flutter (Android و iOS)
- **الإصدار الحالي:** 2.0.1+1

## 2. التقنيات الأساسية (Tech Stack)
- **إطار العمل (Framework):** Flutter (مخصص لـ SDK >=3.3.0 <4.0.0)
- **إدارة الحالة (State Management):** Riverpod (الإصدار 2.x) مع أدوات توليد الأكواد (riverpod_generator, riverpod_annotation) لإدارة حالة حديثة وفعالة.
- **الخوادم وقاعدة البيانات (Backend / BaaS):** 
  - **Firebase Core** لتهيئة الخدمات.
  - **Firebase Auth** لنظام المصادقة.
  - **Cloud Firestore** لتخزين البيانات (المستخدمين والملاحظات).
  - **Firebase Storage** للتخزين السحابي للملفات والوسائط (مؤقتاً قبل الانتقال لـ Mega Cloud بحسب الملاحظات في الكود).

## 3. بنية المشروع (Project Architecture)
يتبع التطبيق معمارية نظيفة تعتمد على الميزات (Feature-First Architecture) مع تقسيم للمجلدات بالشكل التالي:

- **`lib/core/` (الأساسيات):** 
  - `constants/`: الثوابت العامة للتطبيق.
  - `errors/`: معالجة الأخطاء والاستثناءات.
  - `services/`: الخدمات (مثل خدمة الإشعارات).
  - `theme/`: مظهر التطبيق (يحتوي على مزود `theme_provider.dart` لتبديل المظاهر).
  - `utils/`: دوال ومسارات مساعدة.
  
- **`lib/features/` (الميزات):**
  - `auth/`: المصادقة (شاشات تسجيل الدخول، إنشاء الحساب، وشاشة البداية Splash).
  - `home/`: الواجهة الرئيسية للتطبيق.
  - `note/`: إنشاء، تعديل، قراءة، وحذف الملاحظات (CRUD).
  - `profile/`: إدارة الحساب الشخصي.
  - `categories/`: إدارة تصنيفات ومجلدات الملاحظات.
  - `calendar/`: التقويم (لعرض الملاحظات حسب التواريخ).
  - `search/`: محرك البحث المدمج في التطبيق.
  - `notifications/`: عرض وإدارة التنبيهات الخاصة بالمستخدم.

## 4. الميزات الرئيسية (Key Features)
1. **المصادقة:** تسجيل دخول وإنشاء حسابات بطرق آمنة (يتم التحقق من النماذج باستخدام حزمة `formz`).
2. **الملاحظات السحابية:** تخزين وتحديث الملاحظات في الوقت الفعلي باستخدام قاعدة بيانات Firestore.
3. **الإشعارات المحلية:** التنبيهات المجدولة والمحلية باستخدام `flutter_local_notifications` وتحديد المنطقة الزمنية عبر `timezone`.
4. **التصنيف والبحث:** تنظيم شامل للملاحظات يسهل استرجاعها عبر ميزات البحث والتصنيفات.
5. **التقويم:** دمج الملاحظات برزنامة التطبيق لإدارة المواعيد والمهام المرتبطة بأيام محددة.
6. **الوضع الفاتح والداكن:** دعم متقدم لـ Material 3، مع توفير وضع داكن بلون أساسي (Modern Green/Blue: #29B1F4) وخلفيات مناسبة.

## 5. الحزم والإضافات المستخدمة (Dependencies)
- **البرمجة الوظيفية وهندسة النماذج (Architecture & Models):** 
  - `dartz`: للبرمجة الوظيفية والتعامل مع حالات النجاح/الفشل بأسلوب نظيف.
  - `freezed` & `json_annotation`: لبناء نماذج البيانات (Models) غير القابلة للتغيير (Immutable) بشكل آلي.
- **تخزين البيانات والملفات محلياً (Local Storage):** 
  - `shared_preferences`: لحفظ التفضيلات البسيطة.
  - `path_provider`: لإدارة مسارات حفظ الملفات على النظام.
- **واجهة المستخدم والوسائط (UI/UX):** 
  - `google_fonts`: لتخصيص الخطوط.
  - `share_plus`: لمشاركة الملاحظات خارج التطبيق.
  - حزم أيقونات وشاشة البداية: `flutter_launcher_icons`, `flutter_native_splash`.
  
## 6. ملاحظات وتفاصيل أخرى
- تم استخدام إعدادات بيئة محددة كـ "Private App" مما يعني عدم النشر بالغلط على منصة pub.dev.
- يركز التطبيق على تجربة المستخدم (UX) من خلال استخدام رسوميات سلسة وتحميل مسبق للبيانات عبر Riverpod.
- الكود مبني بشكل قابل للتوسع لتسهيل إضافة ميزات لاحقة مثل التخزين المتقدم عبر Mega Cloud.
